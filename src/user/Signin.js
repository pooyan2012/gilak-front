import React from "react";
import Layout from "../core/Layout";

const Signin = () => (
    <Layout title="Signin" description="This is signin description">
      ...
    </Layout>
  );

export default Signin;
